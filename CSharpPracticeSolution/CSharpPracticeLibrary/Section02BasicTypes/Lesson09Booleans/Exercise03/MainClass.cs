namespace CSharpPracticeLibrary.Section02BasicTypes.Lesson09Booleans.Exercise03;
public static class MainClass
{
    public static bool IsAccountDisabled(bool isActive)
    {
        return isActive == false; //don't like the other way.
    }
}