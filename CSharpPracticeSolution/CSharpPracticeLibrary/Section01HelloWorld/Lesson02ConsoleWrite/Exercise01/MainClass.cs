namespace CSharpPracticeLibrary.Section01HelloWorld.Lesson02ConsoleWrite.Exercise01;
public static class MainClass
{
    public static void PrintMessage()
    {
        //will see if it fails because i did not use Console Write first (well see).
        Console.Write("C# is ");
        Console.WriteLine("fun!");
    }
}